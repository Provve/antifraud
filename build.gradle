plugins {
    id 'maven-publish'
}

publishing {
    publications {
        apiSpec(MavenPublication) {
            artifactId = 'antifraud-api'
            group = 'tech.provve.specs'
            version = '0.0.1'
            artifact source: '/src/antifraud-api.yaml'
        }
    }
    repositories {
        mavenLocal()
    }
}